{
  "name": "vericare-frontend",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=static/bundle.js --format=iife --minify",
    "dev": "esbuild src/main.ts --bundle --outfile=static/bundle.js --format=iife --watch"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "marked": "^18.0.2"
  }
}
